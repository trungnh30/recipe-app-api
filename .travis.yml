os: 
  - windows
  - linux
  - osx
language: python
python:
  - "3.6"

services:
  - docker

install: 
  - pip install -r requirements.txt

before_script: 
  - pip install docker-compose

script: 
  - docker-compose run app "python3.6 manage.py test && flake8" 